# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json

# Generated RAG index (rebuild with: python -m rag index) — source of truth is chunks.jsonl
app/adtc-bench-workspace/rag_index/
